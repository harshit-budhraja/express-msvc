const { utilities } = global;
const { logger } = utilities;

/**
 * @todo
 * 
 * Write a generic error handler which could
 * be plugged into any catch-block and it drives
 * loggings, throw behaviour for them etc.
 */